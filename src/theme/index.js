import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',  // Cambia este valor según tu preferencia
    },
    secondary: {
      main: '#dc004e',
    },
  },
});

export default theme;
